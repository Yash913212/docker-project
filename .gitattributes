# Text files should use LF line endings
* text=auto eol=lf

# Shell scripts must use LF
*.sh text eol=lf

# Python files should use LF
*.py text eol=lf

# Cron files must use LF (Unix format)
cron/2fa-cron text eol=lf

# Docker files use LF
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# PEM files (keys) are text
*.pem text eol=lf

# Requirements file
requirements.txt text eol=lf

